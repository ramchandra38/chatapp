{
  "name": "react-chat-tutorial",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "npm": "5.7.1",
    "node": "9.5.0"
  },
  "dependencies": {
    "@pusher/chatkit-client": "^1.12.1",
    "@pusher/chatkit-server": "^2.1.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  },
  "scripts": {
  "start": "concurrently \"node ./server.js\" \"react-scripts start\"",
    "build": "concurrently \"node ./server.js\" \"react-scripts build\"",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
